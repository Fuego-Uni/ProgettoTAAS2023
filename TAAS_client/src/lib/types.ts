export type ContentPreviewData = {
  banner: string,
  title: string,
  date: string,
  rating_average: number,
  rating_personal: number,
}

export type UserInfo = {
  name: string,
  email: string,
  role: string,
}